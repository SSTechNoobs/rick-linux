[[ -f ~/.bashrc ]] && . ~/.bashrc

# Ricks Linux automatic Hyprland startup
if [[ "$(tty 2>/dev/null)" == "/dev/tty1" && -z "${DISPLAY:-}" && -z "${WAYLAND_DISPLAY:-}" ]]; then
    exec start-hyprland
fi
